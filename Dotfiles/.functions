# Functions for days
md() {
    mkdir "$1"
    cd "$1"
}

gco() {
    if [ -z "$1" ]; then
        git commit -v
    else
        git commit -m "$1"
    fi
}

gca() {
    git add --all && gco "$1"
}

gcap() {
    gca "$1" && git push
}
